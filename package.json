{
  "name": "ace",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest",
    "prettier-check": "prettier --single-quote --trailing-comma es5 --arrow-parens always --check 'src/**/*.js'",
    "prettier-format": "prettier --single-quote --trailing-comma es5 --arrow-parens always --write 'src/**/*.js'"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "jest": "^24.8.0",
    "prepush": "^3.1.11",
    "prettier": "^1.18.2"
  },
  "prepush": [
    "npm test",
    "npm run prettier-check"
  ]
}
